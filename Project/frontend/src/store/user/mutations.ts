import { MutationTree } from 'vuex';
import { ExampleUserInterface } from './state';

const mutation: MutationTree<ExampleUserInterface> = {
  someMutation(/* state: ExampleUserInterface */) {
    // your code
  },
};

export default mutation;
